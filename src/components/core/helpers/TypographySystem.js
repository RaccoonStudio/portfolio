const TypographySystem = {};

TypographySystem.lineheight = {
  comfortable: '1.5',
};

TypographySystem.sizes = {
  defaultSize: '1.6rem',
  blogContent: '1.8rem',
  smallContent: '1.4rem',
};

TypographySystem.stack = {
  defaultStack: 'Montserrat, Helvetica, Arial, sans-serif',
  titleStack: 'Playfair Display, Times New Roman, serif',
};

TypographySystem.weights = {
  hairstyle: 100,
  thin: 200,
  light: 300,
  default: 400,
  bold: 600,
  bolder: 700,
};

export default TypographySystem;
